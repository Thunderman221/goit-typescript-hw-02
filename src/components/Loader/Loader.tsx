import { ClipLoader } from "react-spinners";

const Loader: React.FC = () => {
  return (
    <div>
      <ClipLoader color="blue" cssOverride={{}} size={35} />
    </div>
  );
};

export default Loader;
